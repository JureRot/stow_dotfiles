todo:
- update treesitter to main brainc (from master) (this is a breaking change)
- stop using mason use just native nvim-lspconfig
- move language configs (lua, python, julia) into its specific files
